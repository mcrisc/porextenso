apply plugin: 'java'

version = 1.0

repositories {
  mavenCentral()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

jar {
  manifest {
    attributes("Implementation-Title": "PorExtenso",
               "Implementation-Version": version)
  }
}
